<section class="volt-section" style="min-height: 55vh; display: flex; align-items: center;">
  <div class="volt-section__inner" style="text-align: center;">
    <p class="volt-kicker" style="justify-content: center;">{{ 'templates.404.kicker' | t }}</p>
    <h1 class="volt-title">{{ 'templates.404.title' | t }}</h1>
    <p class="volt-lede" style="margin: 0 auto 2rem;">{{ 'templates.404.subtext' | t }}</p>
    <a class="volt-btn volt-btn--primary" href="{{ routes.root_url }}">{{ 'templates.404.cta' | t }}</a>
  </div>
</section>
